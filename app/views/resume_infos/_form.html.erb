<%= form_for(@resume_info) do |f| %>
  <% if @resume_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resume_info.errors.count, "error") %> prohibited this resume_info from being saved:</h2>

      <ul>
      <% @resume_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
